app:
  skill:
    telemetry:
      token: test-skill-telemetry-token
      gitlab:
        token: test-skill-gitlab-token
  seed:
    pilot-skills: false
  scraper:
    ai-tools:
      enabled: false
    mcp:
      enabled: false
    llm-leaderboard:
      enabled: false
    aibase:
      enabled: false

spring:
  datasource:
    url: jdbc:h2:mem:ai_platform_test;DB_CLOSE_DELAY=-1;MODE=MySQL;DATABASE_TO_UPPER=false
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
